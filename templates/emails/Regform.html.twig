<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Nouvel email du Webdev77.fr</title>
    <style>
        /* Styles de base pour les clients de messagerie */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .container {
            width: 100%;
            padding: 20px;
            background-color: #f8f9fa;
        }
        .card {
            border: 1px solid #dee2e6;
            border-radius: 0.25rem;
            background-color: #ffffff;
            box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
        }
        .card-header {
            background-color: #007bff;
            color: #ffffff;
            padding: 0.75rem 1.25rem;
            border-bottom: 1px solid #dee2e6;
            border-radius: 0.25rem 0.25rem 0 0;
        }
        .card-body {
            padding: 1.25rem;
        }
        .list-group-item {
            padding: 0.75rem 1.25rem;
            border: 1px solid #dee2e6;
            border-radius: 0.25rem;
            background-color: #ffffff;
        }
        .list-group-item + .list-group-item {
            border-top: 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="card-header">
                Nouveau courriel du Projet Jury
            </div>
            <div class="card-body">
                <ul class="list-group">
                    <li class="list-group-item"><strong>Email:</strong> {{ data.email}}</li>
                    <li class="list-group-item"><strong>Pseudo:</strong> {{ data.username }}</li>
                    <li class="list-group-item"><strong>Prénom :</strong> {{ data.firstName }}</li>
                    <li class="list-group-item"><strong>Nom :</strong> {{ data.lastName }}</li>
<li class="list-group-item"><strong>Cours :</strong>
    <ul>
        {% if data.courses|length > 0 %}
            {% for course in data.courses %}
                <li>{{ course.name }}</li> <!-- Assuming each Course entity has a 'name' property -->
            {% endfor %}
        {% else %}
            <li>Aucun cours sélectionné.</li> <!-- Display this if no courses are selected -->
        {% endif %}
    </ul>
</li>
                    <li class="list-group-item"><strong>Campus :</strong> {{ data.campus.name }}</li>
                    <li class="list-group-item"><strong>Téléphone:</strong> {{ data.phone}}</li>
                </ul>
            </div>
        </div>
    </div
</body>